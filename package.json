{
	"name": "arenaswap",
	"version": "2.0.0",
	"description": "Watches every live game across 12 leagues and auto-switches your browser tab to the most exciting one every 15 seconds.",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "turbo run dev",
		"build": "turbo run build",
		"build:all": "turbo run build build:firefox build:edge",
		"zip": "turbo run zip",
		"zip:firefox": "turbo run zip:firefox",
		"zip:edge": "turbo run zip:edge",
		"zip:all": "turbo run zip zip:firefox zip:edge",
		"test": "turbo run test",
		"typecheck": "turbo run typecheck",
		"powerscore:simulate": "esbuild scripts/powerscore/simulateDistribution.ts --bundle --platform=node --outfile=scripts/powerscore/simulateDistribution.cjs --log-level=silent && node scripts/powerscore/simulateDistribution.cjs",
		"powerscore:validate-live": "esbuild scripts/powerscore/validateLive.ts --bundle --platform=node --outfile=scripts/powerscore/validateLive.cjs --log-level=silent && node scripts/powerscore/validateLive.cjs"
	},
	"devDependencies": {
		"turbo": "^2.9.16",
		"typescript": "^6.0.3"
	},
	"overrides": {
		"@webext-core/fake-browser": "1.3.4"
	},
	"packageManager": "npm@10.9.0",
	"dependencies": {
		"esbuild": "^0.28.0",
		"skills": "^1.5.7"
	}
}
